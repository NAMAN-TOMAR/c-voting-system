#ifndef MENU_H
#define MENU_H

/* Show the main menu (Admin/User/Exit) */
void showMainMenu();

/* Admin specific menu (add, list, results) */
void showAdminMenu();

/* User specific menu (list, vote) */
void showUserMenu();

#endif

